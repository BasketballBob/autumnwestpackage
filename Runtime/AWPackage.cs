using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace AWP
{
    public static class AWPackage
    {
        public const string PackagePath = "Packages/com.autumnwest.autumnwestpackage";
        public const string RuntimePath = PackagePath + "/Runtime";
        public const string EditorPath = PackagePath + "/Editor";
    }
}
